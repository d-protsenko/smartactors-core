<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>info.smart_tools.smartactors</groupId>
    <artifactId>core-pack</artifactId>
    <version>0.3.3</version>

    <packaging>pom</packaging>

    <properties>
        <core.version>0.3.3</core.version>
        <feature.version>0.3.3</feature.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>
    
    <repositories>
        <repository>
            <id>archiva.smartactors-modules</id>
            <url>http://archiva.smart-tools.info/repository/smartactors-modules/</url>
        </repository>
    </repositories>

    <dependencies>

        <!-- Base -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>base.interfaces.ipool</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>base.pool</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>base.pool-guard</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>base.exception.initialization-exception</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>base.up-counter</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>base.iup-counter</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>base.isynchronous-service</artifactId>
            <version>${core.version}</version>
        </dependency>
             
        <!-- Dump interface -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>dumpable-interface.idumpable</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>dumpable-plugins.make-dump</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- Security -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>security-plugins.password-encoder-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- Scope -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>scope-plugins.scoped-ioc-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>scope-plugins.scope-provider-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>scope-plugins.subscribe-scope-provider-on-scope-creation-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- ConfigurationManager-->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>configuration-manager-plugins.configuration-manager-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>configuration-manager-plugins.read-config-file-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- IObject -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>iobject-plugins.ds-object-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>iobject-plugins.fieldname-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>iobject-plugins.ifieldname-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- IOC -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>ioc-plugins.ioc-keys-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- Field -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>field-plugins.field-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>field-plugins.nested-field-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- IOCStrategyPack -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>ioc-strategy-pack-plugins.resolve-iobject-strategies-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>ioc-strategy-pack-plugins.resolve-standard-types-strategies-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>ioc-strategy-pack-plugins.datetime-formatter-strategy-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>ioc-strategy-pack.resolve-by-composite-name-ioc-with-lambda-strategy</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>ioc-strategy-pack.resolve-by-type-and-name-strategy</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>ioc-strategy-pack.uuid-nextid-strategy</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- Task -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>task-plugins.blocking-queue.blocking-queue-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- FeatureLoader -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>feature-loader-plugins.feature-loader-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>feature-loader-plugins.filesystem-facade-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!--&lt;!&ndash; Database &ndash;&gt;-->
        <!--<dependency>-->
            <!--<groupId>info.smart_tools.smartactors</groupId>-->
            <!--<artifactId>database-plugins.collection-name-plugin</artifactId>-->
            <!--<version>${core.version}</version>-->
        <!--</dependency>-->

        <!--IObject-extension -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>iobject-extension-plugins.configuration-object-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>iobject-extension-plugins.wds-object-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- MessageProcessing -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.actor-receiver-creator-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.chain-call-receiver-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.chain-choice-strategy-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.constant-chain-choice-strategy-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.condition-chain-choice-strategy-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.handler-routing-receiver-creator-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.map-router-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.message-processor-and-sequence-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.messaging-identifiers-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.immutable-receiver-chain-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.receiver-chains-storage-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.receiver-generator-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.standard-object-creators-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.starter-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.wrapper-generator-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.exception-handling-receiver-plugins</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing.exception-handling-receivers</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-interfaces.iresponse-strategy</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.chain-modification-strategies-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing.response-sender-receiver</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.response-sender-receiver-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.message-bus-response-strategy-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing.null-response-strategy</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>message-processing-plugins.null-response-strategy-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!--shutdown-->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>shutdown-plugins.on-shutown-request-configuration-section-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>shutdown-plugins.root-up-counter-plugin</artifactId>
            <version>${core.version}</version>
        </dependency>


        <!-- CoreStarter -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core-service-starter.core-starter</artifactId>
            <version>${core.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.5.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.19.1</version>
                <configuration>
                    <useSystemClassLoader>false</useSystemClassLoader>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <inherited>false</inherited>
                <configuration>
                    <descriptor>bin.xml</descriptor>
                    <finalName>${artifactId}-${pom.version}</finalName>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.8.2</version>
                <goals>
                    <goal>deploy-file</goal>
                </goals>
                <configuration>
                    <generatePom>false</generatePom>
                    <file>${basedir}/target/${artifactId}-${feature.version}-archive.zip</file>
                    <url>http://archiva.smart-tools.info/repository/smartactors-features/</url>
                    <repositoryId>archiva.smartactors-features</repositoryId>

                    <groupId>info.smart_tools.smartactors</groupId>
                    <artifactId>${artifactId}</artifactId>
                    <version>${feature.version}</version>
                    <packaging>zip</packaging>
                    <description>The artifact contains follow list of modules: </description>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
