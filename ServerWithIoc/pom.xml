<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>core</artifactId>
        <groupId>info.smart_tools.smartactors</groupId>
        <version>0.2.0-SNAPSHOT</version>
    </parent>

    <artifactId>core.server_with_ioc</artifactId>

    <dependencies>
        <!-- Interfaces -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.iserver</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- Service locator -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.ioc</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.scope_provider</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.named_keys_storage</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- Implementations of containers -->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.strategy_container</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- Other implementations -->

        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.scope_guard</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.create_new_instance_strategy</artifactId>
            <version>${core.version}</version>
        </dependency>
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.singleton_strategy</artifactId>
            <version>${core.version}</version>
        </dependency>

        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.resolve_by_name_ioc_with_lambda_strategy</artifactId>
            <version>${core.version}</version>
        </dependency>
        <!--<dependency>-->
            <!--<groupId>info.smart_tools.smartactors</groupId>-->
            <!--<artifactId>core.scope_creation_event_handler</artifactId>-->
            <!--<version>${core.version}</version>-->
        <!--</dependency>-->
        <dependency>
            <groupId>info.smart_tools.smartactors</groupId>
            <artifactId>core.iobject_simple_implementation</artifactId>
            <version>${core.version}</version>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>